# Engine packages

Place each supported engine package in its own directory here (or in `engines/`
next to the installed executable). Each package supplies `engine.anyband.json`,
a compatible executable and its own game data. AnybandUI includes no engine.

See `../protocol/README.md` for the required contract. Files in this directory
are local installations, not frontend source or release contents.
