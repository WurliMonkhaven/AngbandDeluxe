cmake_minimum_required(VERSION 3.24)
project(AnybandUI VERSION 1.0 LANGUAGES C CXX)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/game")
if(MSVC)
    add_compile_options(/utf-8)
endif()
file(READ "${CMAKE_CURRENT_SOURCE_DIR}/protocol/full-v1.json" ANYBANDUI_CONTRACT_JSON)
configure_file(protocol/protocol_contract.h.in generated/protocol_contract.h @ONLY)
add_subdirectory(anybandui)
enable_testing()
add_test(NAME client COMMAND anybandui-client-tests "${CMAKE_BINARY_DIR}/test-settings.json")
